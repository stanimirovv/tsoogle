// Extract the directory from tsconfig
// if db doesn't exist, create it, traverse, write to it
// if db exists, read from it
export function doesIndexExist (tsconfigPath: string): boolean {
}

export function fetchFunctionsFrom(tsconfigPath: string): FunctionDetail[] {
}